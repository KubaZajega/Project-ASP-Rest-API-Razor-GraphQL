# Project: ASP.NET Core (REST, Razor, GraphQL)

This repository contains a project developed for the **Backend Application Programming** course. It implements three different architectural patterns within a single .NET application.

## 🚀 Key Features

* **REST API:** Standard CRUD operations with Swagger documentation.
* **GraphQL:** Flexible data querying using HotChocolate.
* **Razor Pages:** Server-side rendered frontend UI.
* **Entity Framework Core:** database management and ORM.

## 🔗 Endpoints

Once the app is running, you can access:

* **Web Interface:** `/` (Home)
* **REST API Docs:** `/swagger`
* **GraphQL Playground:** `/graphql`
